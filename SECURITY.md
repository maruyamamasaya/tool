---
status: active
updated: 2026-09-07
---

# セキュリティ境界

## 現在確認できる境界

- サイトは静的ファイルとして配信され、アプリケーションサーバー、DB、認証・認可、秘密情報、独自 API はリポジトリ内に存在しない。
- 利用者のテキスト、画像、CSV 等は原則としてブラウザ内で処理される。サーバーへ入力データを送信する機能は確認されていない。
- 一部ツール、ルートのお気に入り、カテゴリとツールの表示設定は `localStorage` に保存する。保存値は同じ origin のブラウザに残り、共有端末の別利用者から見える可能性がある。暗号化、端末間同期、サーバー側バックアップはない。
- ファイル import は利用者が選んだローカルファイルを File API 等で読み、export は Blob やダウンロードリンク等を利用する。
- YAML Viewer は `js-yaml@4.1.0` を jsDelivr から実行時に取得する。確認できた唯一の外部実行時依存であり、採用理由と方針上の扱いは不明である。
- Linux Command Explorer の `fetch('commands.json')` は同一サイト内の静的ファイルへの読み込みである。

## 変更時の確認事項

- 利用者入力や保存データを新しい外部宛先へ送らない。必要な場合は、送信内容、宛先、目的、同意、失敗時の挙動を明示し、[ADR-001](decisions/ADR-001-static-browser-tools.md) を再検討する。
- CDN、分析、フォント等の外部実行時依存を暗黙に追加しない。供給網、可用性、プライバシー、固定バージョン、ローカル化の可否を確認する。
- `localStorage` の新規利用では、キー、保存内容、削除方法、破損・容量超過時の挙動、共有端末での露出を検討する。機密情報の保存先として扱わない。
- import データ、URL パラメーター、保存済みデータは信頼せず、構造とサイズを検証する。利用者入力を描画するときは `textContent` 等を優先し、不必要な `innerHTML` を避ける。
- 外部 URL を開く、HTML を生成する、ファイルを出力する機能では、スキーム、エスケープ、ファイル名、MIME type を確認する。
- 認証、権限、サーバー、DB、秘密情報を必要とする変更は現在のシステム境界外であり、実装前に設計判断と運用責任を明確にする。

## 運用上の未確認事項

GitHub Pages の source branch、公開権限、保護ルール、脆弱性報告窓口など、ホスティングおよびリポジトリ設定はコードから確認できない。推測せず [CURRENT.md](CURRENT.md) の Unknowns として扱う。
