# AI 開発ガイド

このファイルは、このリポジトリで作業する AI エージェントの入口です。人向けの利用方法とツール一覧は [README.md](README.md)、現在状態は [CURRENT.md](CURRENT.md)、構成は [ARCHITECTURE.md](ARCHITECTURE.md) を参照してください。

## 作業開始時

1. この `AGENTS.md` を読む。
2. `CURRENT.md` で既知の制約、未解決事項、次のアクションを確認する。
3. `ARCHITECTURE.md` で変更対象の責務とデータフローを確認する。
4. タスクに関係する `decisions/` の ADR と、必要なら直近の `sessions/` を選んで読む。全ファイルを無条件には読まない。
5. 対象ツールの `index.html`、JavaScript、CSS、テストと、ルート一覧への登録状況を調査する。

## 実装時の原則

- 推測だけで変更せず、既存実装・テスト・Git 履歴を一次情報として確認する。コードから意図まで断定できない場合は「不明」と記録する。
- 各ツールが独立して動く現在の静的構成と、ブラウザ内処理を基本とする方針との整合を保つ。
- 無関係な整形、リファクタリング、依存追加を混ぜない。
- 入力データを外部へ送信する変更、外部依存、永続化、権限、セキュリティ境界の変更は特に明示して検討する。エラー回避だけの暫定修正を恒久対応として扱わない。
- ロジックは可能な限り DOM 初期化から分離して `module.exports` し、Node.js 組み込み `assert` で回帰テストを追加する。既存の対象ツールの様式を優先する。
- 新規ツールでは kebab-case のディレクトリを作り、ルート `index.html` と `README.md` の一覧を同期する。
- UI 変更では主要操作、狭い画面、キーボード操作をブラウザでも確認する。

## Definition of Done

タスクの性質に応じ、終了前に次を実施する。存在しない仕組みを導入したり、存在しないコマンドを無理に実行したりしない。

1. 対象の `test.js`（変更が横断的なら全テスト）を実行する。
2. lint / typecheck / build の仕組みが対象時点で存在するなら実行する（現在は未導入。`CURRENT.md` 参照）。
3. UI 変更ならブラウザで確認する。
4. `git diff --check` と `git diff` を確認し、無関係な変更がないことを確かめる。
5. ドキュメント更新要否を判定し、少なくとも `CURRENT.md` の日付・状態・次のアクションを現状に合わせる。
6. 構成やデータフローが変われば `ARCHITECTURE.md` を更新する。
7. 重要かつ理由を確認できる設計判断があれば `decisions/` に ADR を追加する。細かな実装判断は ADR にしない。
8. `sessions/YYYY-MM-DD-<topic>.md` に調査、変更、検証、未解決事項を簡潔に記録する。
9. 未解決事項を `CURRENT.md` に残し、次回の開始点を明確にする。

### 現在利用できる確認コマンド

```bash
# ルート一覧と各ツールの Node.js テスト
for test in site.test.js */test.js; do node "$test"; done

# ローカル表示
python3 -m http.server 8000

# 差分品質
git diff --check
git diff
```
