---
status: active
updated: 2026-08-28
---

# 現在状態

## 要約

- GitHub Pages で公開する、インストール不要のブラウザツール集。ルートの一覧ページには 78 ツールが登録されている。
- ルート直下に 78 個のツールディレクトリがあり、各ディレクトリを個別 URL として公開する。全ディレクトリに `index.html` がある。
- 実行時ビルドとパッケージ管理はなく、HTML / CSS / JavaScript を直接配信する。サーバー、DB、migration、認証、API 定義は存在しない。
- 75 ツールに `test.js` があり、これにルートの `site.test.js` を加えた Node.js テストがある。npm scripts、lint、typecheck、CI ワークフローは存在しない。
- 直近の機能開発は SQL Playground と CSV Editor（2026-08-24）。現在開発中と明示された機能やロードマップは確認できない。

機能の利用者向け一覧は [README.md](README.md)、動作構成は [ARCHITECTURE.md](ARCHITECTURE.md) を正とし、ここでは変化しやすい状態だけを管理する。

## 現在の機能範囲

- ルート一覧はカテゴリ別カード、検索、PC でのミニウィンドウ、最大 10 件のお気に入りを提供する。
- 各ツールは計算、テキスト加工、データ表示、タスク管理などをクライアント内で実行する。
- 一部ツールとルートのお気に入りは `localStorage` に状態を保存する。ファイルの import / export を行うツールもある。
- `linux-command-explorer` は同一ディレクトリの `commands.json` を `fetch` するため、ローカル確認には HTTP サーバーが必要である。

## 既知の制約・不整合

- README の表は 57 ツールで、ルート一覧の 78 ツールを網羅していない。README は価値ある利用者向け資料として維持するが、一覧同期が必要である。
- `site.test.js` はツールカード数を 77 と期待するが、現在の `index.html` には 78 件あるため失敗する。カードとディレクトリの対応自体は 78 件で一致しており、SQL Playground 追加時の期待値更新漏れとみられるが、意図は未確認である。
- README は「外部ライブラリを使わない」と説明している一方、`yaml-viewer/index.html` は jsDelivr から `js-yaml@4.1.0` を読み込む。この例外の意図は履歴から確認できず、オフライン時には YAML Viewer が動作しない。
- `drawing/test` と `json-formatter/test` は名前に `.js` がなく内容も改行のみで、テストとして機能しない。実装漏れか不要ファイルかは不明。
- 自動 CI、lint、typecheck、build は未導入であり、全テストはローカルのシェルループに依存する。
- GitHub Pages の公開設定（ブランチ、カスタム workflow の有無）はリポジトリ内には記録されておらず、ホスティング側設定は確認不能である。

## 現在の優先事項 / 次のアクション

1. `site.test.js` の期待カード数を現状に合わせ、全体テストを green に戻す。
2. 機能変更時には対象テストと全体テストを実行し、ルート一覧と README の同期を保つ。
3. README に未掲載の 21 ツールを、ルート一覧と照合して追記する。
4. YAML Viewer の CDN 依存を許容するか、ローカル化するかをオーナーに確認し、判断できた場合だけ ADR と説明を更新する。
5. 空の拡張子なし `test` 2 件の意図を確認し、テスト追加または削除を別タスクで行う。
6. 必要性と運用主体を確認したうえで、全 Node.js テストを実行する CI の導入を検討する。
