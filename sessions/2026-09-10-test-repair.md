---
status: completed
updated: 2026-09-10
---

# 既知のテスト失敗の修正

## 変更

- ルート一覧が 78 ツールで整合していることと追加履歴を確認し、`site.test.js` の期待件数を 77 から 78 に更新した。
- Meeting Timer が入力時刻をローカル時刻として扱う実装に合わせ、`todayAt` のテストを UTC 固定文字列ではなくローカル日時要素で検証するようにした。
- アプリケーションの挙動は変更していない。

## 検証

- `site.test.js` と 75 個の個別 `test.js` がすべて成功した。
- `git diff --check` が成功した。

## 未解決事項

- 今回はテスト失敗2件だけを対象とし、README の一覧差分、YAML Viewer の CDN 依存、空の拡張子なし `test`、CI 未導入は変更していない。
